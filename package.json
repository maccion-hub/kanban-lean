{
  "name": "sabemsa-kanban-lean-app",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/web",
    "db:migrate": "npm run prisma:migrate -w apps/api",
    "db:studio": "npm run prisma:studio -w apps/api"
  }
}
